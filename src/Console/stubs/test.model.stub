<?php

namespace DummyNamespace;

use Tests\ModelTestCase as TestCase;
use DummyFullModelClass;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\Relations\BelongsToMany;
use Illuminate\Database\Eloquent\Relations\HasMany;
use Illuminate\Database\Eloquent\Relations\HasManyThrough;
use Illuminate\Database\Eloquent\Relations\HasOne;
use Illuminate\Database\Eloquent\Relations\MorphMany;
use Illuminate\Database\Eloquent\Relations\MorphOne;

class DummyClass extends TestCase
{
    /**
     * The model to test.
     */
    protected $model = DummyModelClass::class;

    /**
     * Array of relationships to test.
     *
     * The key is the relationship method name. The value is an array where
     * the first index is the relationship type and the second index is the
     * model it is related to.
     *
     * Example:
     *      $relationships = [
     *          'books' => [HasMany::class, Book::class]
     *      ]
     *
     * Will assert the relationship is of type HasMany, and is related to the Book model.
     *
     * @var array
     */
    protected $relationships = [];
}
